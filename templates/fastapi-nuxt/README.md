# FastAPI + Nuxt Template

This directory is the canonical template authority for the current reference stack.

- `template.yaml` is the inspectable manifest used by `sdd`.
- `source/` is the derived-project snapshot copied by `sdd init`.
- New initialization and maintainer `dev/` flows should treat `templates/fastapi-nuxt/` as the
  only source of truth for this stack.
